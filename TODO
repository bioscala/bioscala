= TODO =

(This is a work in progress, so many todo's...)

- Traitify the Sequence hierarchy, e.g. Gapped and IUPAC may be traits
- Traitify Attribute support - also, as the attributelist is lazy, we
	may do away with passing it down the hierarchy
- Use Property (RDF type), rather then Attribute as a name?
